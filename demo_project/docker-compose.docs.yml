services:
  docs:
    image: demo_project_local_docs
    container_name: demo_project_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./demo_project:/app/demo_project:z
    ports:
      - '9000:9000'
    command: /start-docs
